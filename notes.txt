git pull

docker rm -f resume_website
docker rm -f resume_website_tunnel

docker build -t resume-site .

docker run -d \
  --name resume_website \
  -p 9080:9080 \
  -e DISCORD_WEBHOOK="insert_webhook_here" \
  resume-site

docker compose up -d